/*global require*/
'use strict';

define({
    env: 'development',

    api: {
      // development
      host: 'http://hack4-colorado-clinica.azurewebsites.net',
    },
    debug: true,
    speed: 250,
    // fraction of text message responses that must be 'yes', for goal to be ok
    okThreshold: 0.5
})
